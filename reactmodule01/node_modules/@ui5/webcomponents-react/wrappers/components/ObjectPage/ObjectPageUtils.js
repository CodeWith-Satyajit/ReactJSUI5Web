import { safeGetChildrenArray } from '../../internal/safeGetChildrenArray';
export const getSectionById = (sections, id) => {
    return safeGetChildrenArray(sections).find((objectPageSection) => { var _a; return ((_a = objectPageSection.props) === null || _a === void 0 ? void 0 : _a.id) === id; });
};
export const extractSectionIdFromHtmlId = (id) => {
    if (!id)
        return null;
    return id.replace(/^ObjectPageSection-/, '');
};
export const getLastObjectPageSection = (ref, alwaysSetMargin) => {
    var _a;
    const sections = (_a = ref.current) === null || _a === void 0 ? void 0 : _a.querySelectorAll('[id^="ObjectPageSection"]');
    if (!sections || sections.length < 1) {
        return null;
    }
    if (!alwaysSetMargin && sections.length === 1) {
        if (sections[0].querySelectorAll('[id^="ObjectPageSubSection"]').length === 0) {
            return null;
        }
    }
    return sections[sections.length - 1];
};
