import React, { forwardRef, isValidElement } from 'react';
import { Title } from '../../../webComponents/Title';
const styles = {
    minHeight: '1.5rem',
    display: 'flex',
    alignItems: 'center'
};
export const TitleBar = forwardRef((props, ref) => {
    const { children, titleBarId } = props;
    return (React.createElement("div", { style: styles, ref: ref, id: titleBarId },
        typeof children === 'string' && React.createElement(Title, null, children),
        isValidElement(children) && children));
});
TitleBar.displayName = 'TitleBar';
