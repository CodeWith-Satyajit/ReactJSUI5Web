import React from 'react';
export const DefaultNoDataComponent = ({ noDataText, className, style }) => {
    return (React.createElement("div", { className: className, style: style }, noDataText));
};
