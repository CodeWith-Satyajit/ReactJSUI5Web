import React, { useCallback } from 'react';
import { stopPropagation } from '../../../../internal/stopPropagation';
import { Input } from '../../../../webComponents/Input';
export const DefaultFilterComponent = ({ column }) => {
    var _a;
    const handleChange = useCallback((e) => {
        column.setFilter(e.target.value || undefined);
    }, [column.setFilter]);
    const handleKeyDown = (e) => {
        if (e.key !== 'Enter') {
            stopPropagation(e);
        }
    };
    // todo remove "undefined" check if wc issue has been fixed (https://github.com/SAP/ui5-webcomponents/issues/2616)
    return React.createElement(Input, { onInput: handleChange, value: (_a = column.filterValue) !== null && _a !== void 0 ? _a : '', showClearIcon: true, onKeyDown: handleKeyDown });
};
