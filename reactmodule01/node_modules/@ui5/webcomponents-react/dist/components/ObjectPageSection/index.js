var __rest = (this && this.__rest) || function (s, e) {
    var t = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
        t[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
        for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
            if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
                t[p[i]] = s[p[i]];
        }
    return t;
};
import clsx from 'clsx';
import React, { forwardRef } from 'react';
import { createUseStyles } from 'react-jss';
import styles from './ObjectPageSection.jss';
const useStyles = createUseStyles(styles, { name: 'ObjectPageSection' });
/**
 * Top-level information container of an `ObjectPage`.
 */
const ObjectPageSection = forwardRef((props, ref) => {
    const { titleText, id, children, titleTextUppercase, className, style } = props, rest = __rest(props, ["titleText", "id", "children", "titleTextUppercase", "className", "style"]);
    const classes = useStyles();
    const htmlId = `ObjectPageSection-${id}`;
    const titleClasses = clsx(classes.title, titleTextUppercase && classes.uppercase);
    return (React.createElement("section", Object.assign({ ref: ref, role: "region", className: className, style: style }, rest, { id: htmlId, "data-component-name": "ObjectPageSection" }),
        React.createElement("div", { role: "heading", "aria-level": 3, className: classes.header, "data-component-name": "ObjectPageSectionTitleText" },
            React.createElement("div", { className: titleClasses }, titleText)),
        React.createElement("div", { className: classes.sectionContent },
            React.createElement("div", { className: classes.sectionContentInner, "data-component-name": "ObjectPageSectionContent" }, children))));
});
ObjectPageSection.defaultProps = {
    titleText: '',
    titleTextUppercase: true
};
ObjectPageSection.displayName = 'ObjectPageSection';
export { ObjectPageSection };
